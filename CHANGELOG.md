# Changelog

## 2.0.0

- Rebrand to `pi-xt-advisors` and load TypeScript from `src/`.
- Configure from pi `settings.json` under `advisor`.
- Add `/advisor config` and `/advisor monitor`.
- Persist advisor transcripts next to the observed session in `advisor/`.
- Add `ADVISOR_SYSTEM.md` and `ADVISOR_APPEND_SYSTEM.md`.
